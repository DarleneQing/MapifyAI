# Layout

App shell and shared layout: bottom tab bar, sidebar, nav link, bottom sheet, notification center.

**Components:** BottomTabBar, AppSidebar, NavLink, BottomSheet, NotificationCenter.

**Used by:** All pages.
